# Bug 反馈：公开配置可用模型不随渠道同步

## 现象

1. 在管理后台「模型管理」中添加渠道（含若干模型）并保存后，系统设置页公开配置中的「系统可用模型」（`availableModels`）一直为空，`channels` 相关数据也没有体现。
2. 普通用户登录后无法看到 / 使用管理员配置的付费模型。

## 预期

- 添加启用渠道后，其模型应自动并入公开配置「系统可用模型」，不需要管理员再手动补选。
- 普通用户可以正常看到并使用管理员开放的付费模型。
